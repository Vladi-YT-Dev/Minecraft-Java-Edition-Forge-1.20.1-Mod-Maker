- `[ ]` Create `BlockData.java` model class
- `[ ]` Update `ModMakerApp.java` (GUI) to include a Queue state, JList UI, and Add Block button
- `[ ]` Update `ModGenerator.java` to handle a `List<BlockData>`
- `[ ]` Update `run.sh` to recompile before running, or manual recompile then test
- `[ ]` Update walkthrough artifact
