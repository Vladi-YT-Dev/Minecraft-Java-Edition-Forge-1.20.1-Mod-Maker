# Adding All Minecraft Items to Recipe Menu

This plan incorporates all ~1,200 item/block IDs found in the user-provided `items.json` into the crafting recipe interface.

## User Review Required

> [!IMPORTANT]
> - I will be loading the full list of Minecraft IDs from the file you provided.
> - The application will now have a searchable list of over 1,200 items in the recipe creator.
> - I will use a lightweight manual parser for the JSON to avoid adding new project dependencies.

## Proposed Changes

### Assets

#### [NEW] [items.json](file:///home/vladimir/.gemini/antigravity/scratch/minecraft_mod_maker/src/main/resources/items.json)
- Copy the file from `/home/vladimir/Downloads/items.json`.

### Source Code

#### [MODIFY] [RecipeGridPanel.java](file:///home/vladimir/.gemini/antigravity/scratch/minecraft_mod_maker/src/main/java/com/modmaker/RecipeGridPanel.java)
- Remove the hardcoded `COMMON_ITEMS` array.
- Implement a `loadAllItems()` method that reads the namespaced IDs from `src/main/resources/items.json`.
- Populate the `itemModel` with the full list.
- Ensure the `IconSystem` is called for every item in the browser list.

## Verification Plan

### Manual Verification
1.  Open the "Add Block" tab.
2.  Open the Recipe Editor popup.
3.  Scroll through the item browser to see if items like `minecraft:acacia_boat` (from the new list) are present.
4.  Verify that searching for a less common item (e.g., "shulker") returns results.
