- [x] Add state stacks (undoStack, redoStack) to PixelArtDialog
- [x] Add helper methods `cloneGrid(Color[][])`, `pushState()`, `undo()`, `redo()`
- [x] Hook `pushState()` into `mousePressed` before modifying the grid
- [x] Hook `pushState()` into the existing `Clear` action
- [x] Add Undo/Redo buttons to UI and bind to the methods
- [x] Add hotkey bindings via InputMap/ActionMap for Undo/Redo
