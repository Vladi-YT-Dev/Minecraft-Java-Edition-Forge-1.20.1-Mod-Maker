# Fix Minecraft Mod Generation Error & Add Live Log Popup

The user is experiencing a `java.io.IOException: Gradle build failed with exit code 1`. To help debug this and improve the user experience, we will:
1. Enhance error reporting with detailed logs.
2. Add a **Live Log Popup** to show real-time progress.

## User Review Required

> [!IMPORTANT]
> The mod generator will now show a real-time log window. I'll need to modify both the UI (`ModMakerApp.java`) and the generation logic (`ModGenerator.java`) to support this.

## Proposed Changes

### [Component Name: ModGenerator]

#### [MODIFY] [ModGenerator.java](file:///home/vladimir/.gemini/antigravity/scratch/minecraft_mod_maker/src/main/java/com/modmaker/ModGenerator.java)

1.  **Log Callback**: Update `generateMod` and `buildMod` to accept an optional `java.util.function.Consumer<String>` for real-time logging.
2.  **Capture Gradle Output**: Already implemented in previous step, but will now also pipe it to the consumer.

### [Component Name: ModMakerApp]

#### [MODIFY] [ModMakerApp.java](file:///home/vladimir/.gemini/antigravity/scratch/minecraft_mod_maker/src/main/java/com/modmaker/ModMakerApp.java)

1.  **Log Dialog**: Create a new inner class or helper method to create a non-modal `JDialog` containing a `JTextArea`.
2.  **SwingWorker Update**: Update the `SwingWorker` in `generateMod` to:
    - Show the log dialog before starting.
    - Pass a `Consumer<String>` to `ModGenerator` that updates the dialog's text area using `SwingUtilities.invokeLater`.
    - Close or mark the dialog as "Done" when the process finishes.

## Open Questions

- Should the log dialog automatically close on success? (I'll keep it open but change the title to "Build Finished" so the user can review it).

## Verification Plan

## Open Questions

- Should I also add a "View Log" button to the error dialog? (This would require UI changes in `ModMakerApp.java`). For now, I'll focus on putting the core error message in the exception.

## Verification Plan

### Automated Tests
- I'll create a JUnit-style test case (or a simple main method) that attempts to generate a mod with a known intentional error (e.g. invalid mod ID if not validated elsewhere, or by manually injecting a syntax error) to verify that the detailed error message is captured.

### Manual Verification
- After the code changes, if I can reproduce the "exit code 1" failure in the user's environment, I will be able to see exactly *why* Gradle is failing.
